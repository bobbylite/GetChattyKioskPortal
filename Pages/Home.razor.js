const pingIdentityScripts = window.pingIdentityScripts;

export const addUserInformationWidget = async (serializedOptions) => {
    //await pingIdentityScripts.addDaVinciWidget(serializedOptions);
}

export const dispose = () => {
    console.debug('Disposing objects.');
}
